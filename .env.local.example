# ─────────────────────────────────────────────────────────────────────────────
# Ambiente de desenvolvimento LOCAL
# Copie este arquivo para .env.local e ajuste conforme necessário
# NUNCA commite o arquivo .env.local
# ─────────────────────────────────────────────────────────────────────────────

# Servidor
PORT=3001
NODE_ENV=development

# Banco de dados (aponta para o container mysql do docker-compose)
DB_HOST=mysql
DB_PORT=3306
DB_USER=servija
DB_PASSWORD=Fe230599@
DB_NAME=servija

# JWT
JWT_SECRET=servija-local-dev-secret-TROQUE-em-producao
JWT_EXPIRES_IN=7d

# Email (Gmail SMTP — opcional; sem configuração os emails são ignorados)
# Gere um App Password em: myaccount.google.com/apppasswords (requer 2FA ativo)
SMTP_USER=
SMTP_PASS=
